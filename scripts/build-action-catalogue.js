#!/usr/bin/env node
/**
 * Scan the routes tree and upsert shared.ai_action_catalogue.
 *
 * For now this is a stub — see V2.2 §8.3 for the design (read from OpenAPI).
 * Each route handler should attach an `actionMeta` object that this script
 * can extract.
 */

"use strict";

console.log("TODO: build action catalogue from OpenAPI spec");
